{
  "name": "aura-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Aura Backend repository (API + DB + service foundations)",
  "workspaces": [
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @aura/api",
    "build:shared": "npm run build --workspace @aura/shared",
    "build": "npm run build:shared && npm run build --workspace @aura/api",
    "start": "node services/api/dist/index.js",
    "db:migrate:deploy": "npm run db:migrate:deploy --workspace @aura/api",
    "lint": "npm run lint --workspace @aura/api",
    "typecheck": "npm run build:shared && npm run typecheck --workspace @aura/api",
    "test": "npm run test --workspace @aura/api"
  },
  "engines": {
    "node": ">=20"
  }
}